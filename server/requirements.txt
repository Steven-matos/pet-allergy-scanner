# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0

# Database & Supabase
supabase>=2.3.0
sqlalchemy>=2.0.23
asyncpg>=0.29.0
alembic>=1.13.1

# Pydantic & Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Authentication & Security
# Removed python-jose[cryptography] to eliminate ecdsa dependency (CVE-2024-23342)
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0
cryptography>=42.0.0
pyotp>=2.9.0

# HTTP & Networking
httpx>=0.26.0,<0.29.0
aiohttp>=3.9.0

# File Upload & Processing
python-multipart>=0.0.6
Pillow>=10.0.0
qrcode[pil]>=7.4.2
bleach>=6.0.0

# Rate Limiting & Performance
slowapi>=0.1.9
psutil>=5.9.0

# Testing (not needed for Railway production, but safe to include)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Security Auditing (optional for production)
pip-audit>=2.6.0
